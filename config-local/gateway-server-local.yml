server:
  port: 5000
eureka:
  client:
    serviceUrl:
      defaultZone: http://wugf:123456@localhost:8761/eureka
#配置路由
zuul:
  routes:
   shopping-mall: /mall/**
   sidecar-stock-server: /sidecar/**
